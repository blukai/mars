[workspace]
resolver = "2"
members = [
  "alloc",
  "bitbuf",
  "containers",
  "dropguard",
  "flag",
  "fswatch",
  "hash",
  "rangealloc",
  "varint",
]

[workspace.package]
edition = "2024"

[workspace.dependencies]
allocator-api2 = { version = "0.2.9" }
criterion = { version = "0.5.1", features = ["html_reports"] }
criterion-cycles-per-byte = { version = "0.6.1" }
# workspace
alloc = { path = "alloc" }
bitbuf = { path = "bitbuf" }
containers = { path = "containers" }
dropguard = { path = "dropguard" }
flag = { path = "flag" }
fswatch = { path = "fswatch" }
hash = { path = "hash" }
rangealloc = { path = "rangealloc" }
varint = { path = "varint" }

# workspace above
# ----
# root package below

[package]
name = "mars"
version = "0.0.0"
edition.workspace = true

[lib]
path = "lib.rs"

[dependencies]
alloc = { workspace = true }
bitbuf = { workspace = true }
containers = { workspace = true }
dropguard = { workspace = true }
flag = { workspace = true }
fswatch = { workspace = true }
hash = { workspace = true }
rangealloc = { workspace = true }
varint = { workspace = true }
